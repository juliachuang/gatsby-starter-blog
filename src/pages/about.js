import React from "react"
import Header from "../components/header"

export default () => (
  <div style={{ color: `teal` }}>
    <Header additionalHeaderText="rain" />
    <p>Such wow. Very React.</p>
  </div>
)
